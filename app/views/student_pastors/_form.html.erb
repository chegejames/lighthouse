<%= simple_form_for(@student_pastor) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :pastor %>
    <%= f.association :student %>
    <%= f.input :date %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
